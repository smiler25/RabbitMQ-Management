<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.smiler.rabbitadministration"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="com.smiler.rabbitadministration.ManagementApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_square_orange"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_circle_orange"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.smiler.rabbitadministration.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="com.smiler.rabbitadministration.preferences.PrefActivity"
            android:label="@string/settings"
            />
        <activity
            android:name="com.smiler.rabbitadministration.info.PolicyActivity"
            android:label="@string/policy"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity
            android:name=".profiles.ProfilesActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/profiles"
            />
    </application>

</manifest>