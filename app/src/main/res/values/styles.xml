<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:fitsSystemWindows">true</item>-->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="listRow">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingStart">3dp</item>
        <item name="android:paddingEnd">3dp</item>
    </style>

    <style name="listRowItem">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
        <!--<item name="android:textSize">@dimen/side_panel_row_font_size</item>-->
        <item name="android:padding">3dp</item>
        <item name="android:textColor">@android:color/black</item>
        <!--<item name="android:background">@android:color/white</item>-->
    </style>

    <style name="listRowItemFirst" parent="listRowItem">
        <item name="android:layout_weight">0.8</item>
        <item name="android:textAlignment">textStart</item>
    </style>

    <style name="overviewPanelStyle">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
        <!--<item name="android:textSize">@dimen/side_panel_row_font_size</item>-->
        <item name="android:textColor">@android:color/black</item>
        <item name="android:orientation">vertical</item>
        <item name="android:background">@drawable/overview_panel</item>
    </style>

    <style name="overviewPanelStyleBig" parent="overviewPanelStyle">
        <item name="android:minHeight">75dp</item>
        <item name="android:minWidth">125dp</item>
        <item name="android:maxHeight">100dp</item>
        <item name="android:maxWidth">200dp</item>
    </style>

    <style name="overviewPanelStyleSmall" parent="overviewPanelStyle">
        <item name="android:minHeight">50dp</item>
        <item name="android:minWidth">125dp</item>
        <item name="android:maxHeight">75dp</item>
        <item name="android:maxWidth">200dp</item>
    </style>

    <style name="tableRowChild">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:padding">5dp</item>
        <item name="android:textAlignment">textStart</item>
    </style>

    <style name="ToolBarStyle">
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
        <item name="android:theme">@style/ThemeOverlay.AppCompat.Dark</item>
        <item name="android:background">?attr/colorPrimary</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="windowActionModeOverlay">true</item>
    </style>

</resources>
